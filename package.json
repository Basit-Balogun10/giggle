{
  "name": "giggle",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "pnpm -w install",
    "clean": "rimraf node_modules && rimraf packages/*/node_modules"
  }
}
